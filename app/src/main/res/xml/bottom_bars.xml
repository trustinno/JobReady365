<?xml version="1.0" encoding="utf-8"?>
<tabs>
    <tab
        id="@+id/tab_favorites"
        icon="@drawable/ic_dashboard_black_24dp"
        title="Dashboard"
        inActiveColor="@color/colorblack"
        activeColor="@color/colorWhite"
        barColorWhenSelected="@color/colorback"
        badgeBackgroundColor="#FFFFFF"
        badgeHidesWhenActive="true"
        />
    <tab
        id="@+id/tab_nearby"
        icon="@drawable/ic_add_box_black_24dp"
        title="PostJob"
        activeColor="@color/colorWhite"
        inActiveColor="@color/colorblack"
        barColorWhenSelected="@color/colorback"
        badgeBackgroundColor="#FFFFFF"/>
    <tab
        id="@+id/tab_friends"
        icon="@drawable/ic_person_black_24dp"
        title="Expired"
        activeColor="@color/colorWhite"
        inActiveColor="@color/colorblack"
        barColorWhenSelected="@color/colorback"
        badgeBackgroundColor="#FFFFFF"/>

    <tab
        id="@+id/tab_bla"
        icon="@drawable/ic_search_black_24dp"
        title="Search"
        activeColor="@color/colorWhite"
        inActiveColor="@color/colorblack"
        barColorWhenSelected="@color/colorback"
        badgeBackgroundColor="#FFFFFF"/>

    <tab
        id="@+id/tab_search"
        icon="@drawable/ic_menu_black_24dp"
        title="More"
        activeColor="@color/colorWhite"
        inActiveColor="@color/colorblack"
        barColorWhenSelected="@color/colorback"
        badgeBackgroundColor="#FFFFFF"/>

</tabs>